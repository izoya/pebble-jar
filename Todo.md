## Transactions
- Fetch and process pending transactions from `transactions/pending` Akahu endpoint. Currently the app uses Akahu's `/transactions` endpoint that only returns completed transactions. 


## Accounts
